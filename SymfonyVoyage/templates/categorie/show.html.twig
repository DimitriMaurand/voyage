{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
   <h2 class="text-xl font-bold text-center m-4">Catégorie</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom_categorie</th>
                <td>{{ categorie.nomCategorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_index') }}"><button  class="  w-auto bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-md">Retour</button></a>

    <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}"><button class="btn w-20 bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 ml-2 rounded-md m-2">editer</button></a>


    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
