{% extends 'base.html.twig' %}

{% block title %}Voyage{% endblock %}

{% block body %}

    <h2 class="text-xl font-bold text-center m-4">Voyage</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voyage.id }}</td>
            </tr>
            <tr>
                <th>Titre_voyage</th>
                <td>{{ voyage.titreVoyage }}</td>
            </tr>
            <tr>
                <th>Description_voyage</th>
                <td>{{ voyage.DescriptionVoyage }}</td>
            </tr>
            
            
            <tr>
                <th>Prix_voyage</th>
                <td>{{ voyage.prixVoyage }}</td>
            </tr>
        </tbody>
    </table>

     <a href="{{ path('app_voyage_index') }}"><button  class="  w-auto bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-md">Retour</button></a> 
<a href="{{ path('app_voyage_edit', {'id': voyage.id}) }}"><button class="btn w-20 bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 ml-2 rounded-md m-2">editer</button></a>
    
{{ include('voyage/_delete_form.html.twig') }}
    
{% endblock %}
