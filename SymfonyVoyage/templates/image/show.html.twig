{% extends 'base.html.twig' %}

{% block title %}Image{% endblock %}

{% block body %}
    <h2 class="text-xl font-bold text-center m-4">Image</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
            <tr>
                <th>Nom_image</th>
                <td>{{ image.nomImage }}</td>
            </tr>
            <tr>
                <th>Url_image</th>
                <td>{{ image.urlImage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_image_index') }}"><button  class="  w-auto bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-md">Retour</button></a>

    <a href="{{ path('app_image_edit', {'id': image.id}) }}"><button class="btn w-20 bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 ml-2 rounded-md m-2">editer</button></a>

    {{ include('image/_delete_form.html.twig') }}
{% endblock %}
